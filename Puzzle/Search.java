package Puzzle;

/**used interface to refer and create an object from it in solver class i tried putting it in DFS class so it would just run as a function but it doesn't work idk why but it is working now took a lot of search **/
public interface Search {

	void search(); //search interface to help with UI when creating a Search.
}
